'use strict';

var _ = require('../');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

it('should export correctly', function () {
  expect(_2.default).toBeDefined();
  expect(_.sweetalert).toBeDefined();
  expect(_.swal).toBeDefined();
  expect(_.close).toBeDefined();
  expect(_.reducer).toBeDefined();
  expect(_.immutableReducer).toBeDefined();
  expect(_.actions).toEqual({
    sweetalert: _.sweetalert,
    swal: _.swal,
    close: _.close,
    closeIfShow: _.closeIfShow
  });
});