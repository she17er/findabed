'use strict';

var _createReducer = require('../createReducer');

var _createReducer2 = _interopRequireDefault(_createReducer);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var INIT_ACTION = { type: '@@test/INIT_ACTION' };

it('should return initial state (number) when be called first time', function () {
  var initialState = 1;
  var reducer = (0, _createReducer2.default)(initialState, { a: function a() {} });
  expect(reducer(undefined, INIT_ACTION)).toEqual(initialState);
});

it('should return initial state (object) when be called first time', function () {
  var initialState = {
    key: 1
  };
  var reducer = (0, _createReducer2.default)(initialState, { a: function a() {} });
  expect(reducer(undefined, INIT_ACTION)).toEqual(initialState);
});

it('should return initial state (array) when be called first time', function () {
  var initialState = [1, 2, 3, 4];
  var reducer = (0, _createReducer2.default)(initialState, { a: function a() {} });
  expect(reducer(undefined, INIT_ACTION)).toEqual(initialState);
});